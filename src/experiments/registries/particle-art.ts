import type { ExperimentLoader } from '../types'

export const particleStageLoaders = {} satisfies Record<string, ExperimentLoader>
export const particlePreviewLoaders = {} satisfies Record<string, ExperimentLoader>
