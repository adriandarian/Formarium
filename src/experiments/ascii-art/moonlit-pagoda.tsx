import { useEffect, useRef } from 'react'
import type { ExperimentRendererProps } from '../types'

export default function MoonlitPagoda({mode='stage'}:ExperimentRendererProps){const ref=useRef<HTMLCanvasElement>(null)
 useEffect(()=>{const c=ref.current,g=c?.getContext('2d');if(!c||!g)return;const compact=mode==='preview';let t=0,raf=0
 const draw=()=>{const b=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,compact?1.1:1.6);c.width=b.width*d;c.height=b.height*d;g.setTransform(d,0,0,d,0,0);g.fillStyle='#03040a';g.fillRect(0,0,b.width,b.height);const fs=Math.max(8,Math.min(b.width/70,b.height/44));g.font=`${fs}px ui-monospace,monospace`;g.textAlign='center';g.textBaseline='middle';const cols=Math.ceil(b.width/fs),rows=Math.ceil(b.height/fs)
 for(let iy=0;iy<rows;iy++)for(let ix=0;ix<cols;ix++){const x=ix*fs,y=iy*fs;const nx=ix/cols,ny=iy/rows;let ch='',alpha=0,color='190,205,255';const moon=(nx-.76)**2+(ny-.19)**2;if(moon<.012){ch=moon<.008?'@':'o';alpha=.72}else{const mountain=.44+.08*Math.sin(nx*10)+.04*Math.sin(nx*25);if(ny>mountain&&ny<.58){ch=['^','/','\\','.'][(ix+iy)%4];alpha=.22}const cx=Math.abs(nx-.49);if(ny>.24&&ny<.69&&cx<.18){const level=Math.floor((ny-.26)/.09);const half=.07+level*.022;if(cx<half&&Math.abs((ny-.26)% .09)<.025){ch=ix%2?'=':'_';alpha=.72;color='235,180,150'}if(cx<.045&&ny>.31){ch=iy%3?'|':'#';alpha=.65;color='220,160,130'}}if(ny>.72){const wave=Math.sin(ix*.7+t*2+iy)*.5+.5;ch=wave>.65?'~':wave>.35?'-':'.';alpha=.18+wave*.18;color='120,170,220'}}if(ch){g.fillStyle=`rgba(${color},${alpha})`;g.fillText(ch,x,y)}}t+=.012;raf=requestAnimationFrame(draw)};draw();return()=>cancelAnimationFrame(raf)},[mode]);return <canvas ref={ref} style={{width:'100%',height:'100%',display:'block'}} aria-label="Moonlit Pagoda ASCII artwork"/>}
