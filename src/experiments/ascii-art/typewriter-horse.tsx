import { useEffect, useRef } from 'react'
import type { ExperimentRendererProps } from '../types'

export default function TypewriterHorse({mode='stage'}:ExperimentRendererProps){const ref=useRef<HTMLCanvasElement>(null)
 useEffect(()=>{const c=ref.current,g=c?.getContext('2d');if(!c||!g)return;const compact=mode==='preview';let t=0,raf=0
 const draw=()=>{const b=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,compact?1.1:1.6);c.width=b.width*d;c.height=b.height*d;g.setTransform(d,0,0,d,0,0);g.fillStyle='#050403';g.fillRect(0,0,b.width,b.height);const fs=Math.max(8,Math.min(b.width/74,b.height/42));g.font=`${fs}px ui-monospace,monospace`;g.textAlign='center';g.textBaseline='middle';const cols=Math.ceil(b.width/fs),rows=Math.ceil(b.height/fs);const phase=t*2
 for(let iy=0;iy<rows;iy++)for(let ix=0;ix<cols;ix++){const nx=ix/cols,ny=iy/rows;const x=(nx-.52)*2.2,y=(ny-.53)*2.2;const body=((x+.05)/.62)**2+(y/.28)**2<1;const neck=((x-.4)/.2)**2+((y+.22)/.38)**2<1;const head=((x-.58)/.25)**2+((y+.48)/.16)**2<1;const tail=x<-.55&&x>-.95&&Math.abs(y+.05-.18*Math.sin((x+1)*5+phase))<.08;const leg1=Math.abs(x+.28)<.07&&y>.2&&y<.72+.12*Math.sin(phase);const leg2=Math.abs(x-.18)<.07&&y>.2&&y<.68+.14*Math.sin(phase+Math.PI);const limb=Math.abs(y-(.3+.7*(x+.45)))<.055&&x>-.45&&x<0;const inside=body||neck||head||tail||leg1||leg2||limb;if(inside){const chars='.:;+*#%@';const v=Math.min(7,Math.floor((1-Math.abs(y)*.7)*8));g.fillStyle=`rgba(245,225,195,${.45+v*.06})`;g.fillText(chars[v],ix*fs,iy*fs)}else if(ny>.72&&Math.sin(ix*1.7+t*4+iy)>.72){g.fillStyle='rgba(220,180,130,.22)';g.fillText(ix%2?'.':',',ix*fs,iy*fs)}}t+=.014;raf=requestAnimationFrame(draw)};draw();return()=>cancelAnimationFrame(raf)},[mode]);return <canvas ref={ref} style={{width:'100%',height:'100%',display:'block'}} aria-label="Typewriter Horse ASCII artwork"/>}
